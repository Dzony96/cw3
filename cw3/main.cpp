#include <iostream>
#include <fstream>

using namespace std;
struct Probka{
double t
double x
Probka(double_t, double_x)
    {t=_t;
    x=_x;}
};

vector <Probka> wczytaj (string)
int main(int argc, char* argv[])
{
    if (argc != 2) {
throw runtime_error(sygnal.csv);
}
string nazwa_pliku = argv[1];
cout << ”Odczytuje plik: ” << nazwa_pliku << endl;
}
